#Matt's take on Brian's .vim setup#

ymmv

# Quick Start #
    git clone git@github.com:mmiermans/dotvim.git ~/.vim
    ln -sf ~/.vim/.vimrc ~/.vimrc
    cd .vim
    git submodule init
    git submodule update
